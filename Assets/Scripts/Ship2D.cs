using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ship2D : MonoBehaviour {

	public float hSpeed, vSpeed, zSpeed, pitchAngle, pitchSpeed, rollAngle, rollSpeed, fireRate, integrity;
	public Transform gun;
	public Shot shot;
}
